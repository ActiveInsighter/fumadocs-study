name: Check vocabulary responsive layout

on:
  push:
    branches:
      - codex/vocabulary-responsive-layout
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Test and build vocabulary layout
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout branch
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          package-manager-cache: false

      - name: Restore dependencies
        id: node-modules-cache
        uses: actions/cache@v5
        with:
          path: node_modules
          key: ${{ runner.os }}-node22-modules-v1-${{ hashFiles('package-lock.json') }}

      - name: Restore npm cache
        if: steps.node-modules-cache.outputs.cache-hit != 'true'
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node22-npm-v1-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit --no-fund

      - name: Refresh generated source
        run: npm run postinstall

      - name: Run vocabulary layout tests
        run: npm test -- tests/word-cards.test.tsx tests/vocabulary-responsive-layout.test.ts

      - name: Build static documentation package
        run: npm run build:static-docs
