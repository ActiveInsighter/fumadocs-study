---
title: 其他词频词卡
description: 410 张补充词卡，按重要性顺序拆成 3 个可复习单元。
---

import { supplementalWordModules } from '../../../../data/vocabulary/supplemental-word-cards.mjs';

这组卡片补充主词表之外的高频阅读与写作词汇。数据保留原始音标、词性释义、义项频次、例句与中文翻译，并按原始顺序拆成 3 个单元。

<Callout type="info" title="建议用法">
  先用精简模式建立识别速度，再切换完整模式查看多义词的频次和例句；把鼠标移到例句上即可查看中文翻译。
</Callout>

<Cards>
  {supplementalWordModules.map((module) => (
    <Card
      key={module.slug}
      href={`/docs/english/supplement-vocabulary/${module.slug}`}
      title={module.title}
      description={module.description}
    >
      {module.count} 张卡片 · {module.firstWord} → {module.lastWord}
    </Card>
  ))}
</Cards>

## 复习路径

按模块顺序复习即可保持原始重要性梯度；遇到熟词生义时切换完整模式，结合义项次数和例句判断具体语境。
