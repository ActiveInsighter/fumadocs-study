---
title: "计算机性能指标"
---

> **复习优先级：高。**
在选择题中会直接考查，也会在解答题间接考查，每个计算机性能指标都要深入掌握。

> **真题考察**
> - 选择题：2011 年第 19 题、2014 年第 12 题、2019 年第 16 题、2020 年第 12 题、2021 年第 12 题、2022 年第 12 题、2023 年第 12 题、2025 年第 18 题
> - 解答题：2009 年第 43 题、2012 年第 43 题、2013 年第 43 题、2016 年第 44 题、2018 年第 43 题
>
> 真题题号仅用于标记考查范围，相关外部题库链接已移除。

### CPU 指标

#### 时钟周期

**时钟周期**（Clock Cycle）是同步数字电路相邻两个有效时钟边沿之间的时间间隔，通常记为 $T_{clk}$。CPU 内部大量时序逻辑都以时钟边沿为基准推进状态。

计算机内部有一个 **晶体振荡器**，通常采用石英晶体，利用其压电效应产生规律的 脉冲信号
。石英晶体在施加电压时会发生机械振动，而机械振动反过来又会产生电压。通过在电路中合理配置电容和电感元件，晶体振荡器能够在特定频率上持续振荡，形成稳定的机器脉冲源。

<InlineSvg src="/images/computer-organization/computer-organization-overview-performance-001.svg" alt={"C 0 C 1 L 1 R 1"} />

**原始的脉冲信号** 是一个连续的 **模拟信号**，在计算机系统中，这个模拟信号通常不会直接使用。为了适配数字电路的需要，**晶体振荡器** 产生的 **模拟信号** 会经过整型和分频电路处理，转换为方波形式的 **数字脉冲信号**，用于同步计算机中各个硬件组件的操作。经过整形与分频后得到稳定的数字时钟。相邻两个同类型有效边沿（例如两个上升沿）之间的时间就是一个 **时钟周期**。

<InlineSvg src="/images/computer-organization/computer-organization-overview-performance-002.svg" alt={"原始连续模拟信号 离散数字信号 整型和分频"} />

**注意**
**计算机中所有的设备都使用相同的时钟周期么？**

**不一定**。某些组件（如 **CPU** 和 **内存** 等）需要同步工作，但不同的设备可能使用不同的时钟周期。

不同部件可能处于不同的时钟域。处于同一时钟域的逻辑由统一时钟协调；跨时钟域通信则需要额外的同步、握手或缓冲机制。

#### 主频

**主频**（CPU Frequency），是指中央处理器（**CPU**）的 时钟频率
（Clock Frequency），表示 **CPU** 每秒可以执行多少个基本 [时钟周期](/docs/408/知识点总结2/computer-organization/overview/performance/#%e6%97%b6%e9%92%9f%e5%91%a8%e6%9c%9f)。**时钟周期** 和 **主频** *互为倒数*：

$$
\mathrm{Frequency}=\frac{1}{Clock Cycle}
$$

其单位通常是 **MHz**（兆赫）或 **GHz**（千兆赫）。它决定了 **时钟振荡** 的快慢：

- **1 Hz**：每秒
  $1$
  次 **时钟振荡**
- **1 MHz** =
  $10^{6}$
  次/秒
- **1 GHz** =
  $10^{9}$
  次/秒

比如，一个 **3.0 GHz** 的 **CPU**，主频是 **3,000,000,000 Hz**，意味着它每秒经历 *30 亿* 个 **时钟周期**。

#### 指令执行指标

> **真题考察**
> - 选择题：2011 年第 12 题、2013 年第 12 题、2026 年第 20 题
>
> 真题题号仅用于标记考查范围，相关外部题库链接已移除。

**CPI**

**CPI**（Cycles Per Instruction）表示平均每条指令需要多少个 **时钟周期**，是一个平均性能指标。CPI 与“指令周期（Instruction Cycle）”不是同一个概念：前者是统计量，后者描述一条指令从取指到执行完成的过程。

下图展示了 **指令周期**、**机器周期** 和 **时钟周期** 三者的关联：

<InlineSvg src="/images/computer-organization/computer-organization-overview-performance-003.svg" alt={"时钟 周期 机器周期 （取指令） 机器周期 （译码） 机器周期 （执行） 机器周期 （执行） 指令周期"} />

在传统教材模型中，可以把三者理解为不同抽象层次：

- **时钟周期**：同步电路的基本节拍。
- **机器周期**：完成一次相对完整的基本操作所需的时间，可能包含若干时钟周期。
- **指令周期**：一条指令从取指到执行结束的全过程，可能包含若干机器周期。

现代流水线处理器中，多条指令的不同阶段会重叠执行，因此不能简单用固定的“机器周期层层相加”来描述真实性能；性能分析通常使用指令数、CPI 与时钟频率。

**IPC**

**IPC**（Instruction Per Cycle）：也称 **每周期指令**，即每个 **时钟周期** 执行的平均指令数，是 **CPI** 的 *倒数*。

$$
\mathrm{IPC}=\frac{1}{\mathrm{CPI}}
$$

**IPS**

**IPS**（Instruction Per Second）：也称 **每秒指令数**，即每秒平均能够执行多少条指令。

$$
\mathrm{IPS}=\frac{\text{主频}}{\mathrm{CPI}}
$$

**MIPS**

**MIPS**（Million Instructions Per Second）表示每秒执行多少百万条指令。它只能在指令集与程序特征相近时进行粗略比较，因为不同 ISA 的“一条指令”完成的工作量可能不同。

$$
1\mathrm{MIPS}=10^{6}\mathrm{IPS}
$$

#### 浮点运算指标

**FLOPS**

**FLOPS**（Floating-point Operations Per Second，每秒浮点运算次数）是衡量计算机浮点运算性能的指标，它表示处理器每秒能执行的浮点运算次数，通常用来评估 **GPU** 或 **AI 芯片** 的性能。

- **MFLOPS**（Million FLOPS）: 百万（
  $10^{6}$
  ）次每秒
- **GFLOPS**（Giga FLOPS）: 十亿次（
  $10^{9}$
  ）每秒
- **TFLOPS**（Tera FLOPS）: 万亿次（
  $10^{12}$
  ）每秒
- **PFLOPS**（Peta FLOPS）: 千万亿次（
  $10^{15}$
  ）每秒
- **EFLOPS**（Exa FLOPS）: 百亿亿次（
  $10^{18}$
  ）每秒
- **ZFLOPS**（Zetta FLOPS）: 万亿亿次（
  $10^{21}$
  ）每秒

每次指标提升都是
$\times10^{3}$
，比如
$\mathrm{GFLOPS}=10^{3}\mathrm{MFLOPS}$
。

<InlineSvg src="/images/computer-organization/computer-organization-overview-performance-004.svg" alt={"FLOPS_units"} />

### 计算机指标

#### 字长

**字长**（Word Length）指的是计算机在内部一次操作中所能处理的 **二进制数字位数**。例如，一个 **32 位** 的 **CPU** 一次可以处理 **32 位** 的数据。

数据通路带宽

**数据通路带宽**（Data Bus Width） 是 **CPU** 和 **内存** 之间传输数据的宽度，通常以 **位** 为单位表示。比如，**64 位** 的数据总线意味着每个 **时钟周期** 内，可以传输 **64 位** 的数据。

#### 主存容量

**主存储器** 能够存储信息的最大容量，通常用 **字节** 来衡量。

**MAR** 的 **位数** 反映了 **可寻址范围** 的最大值（不一定是存储器的实际容量），与 **地址总线** 的 **位数** 一致。

这里主要看清题目中的 **按字节寻址** 和 **按字寻址**，内存空间 / 寻址单位 = 可寻址范围，通过 可寻址范围 计算 **MAR** 的 **位数**。

比如主存地址空间大小为 128KB=
$2^{17}$
B，字长16 位（2B），按字寻址，则 MAR 位数为16。

#### 吞吐量

**吞吐量**（Throughput）表示在 **单位时间** 内，系统可以完成的 **任务数量**。在计算机网络中，它可能表示 **数据的传输速率**；在 **计算** 中，它可能表示 **每秒执行的操作数**。
