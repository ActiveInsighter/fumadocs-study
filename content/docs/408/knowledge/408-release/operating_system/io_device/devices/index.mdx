---
title: "I/O 设备"
---

# I/O 设备

💡
低优先级

低频考点，对于本节重点掌握 I/O 软件层次的概念，I/O 控制方式的部分建议和计算机组成原理中的 [I/O 系统](https://csgraduates.com/constitution_principle/bus/io/) 共同学习。

### I/O 接口类型

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-001.svg" alt={"I/O 设备类型分类 块设备 (Block) 固定大小数据块 支持随机访问 高数据传输速率 机械硬盘 HDD 固态硬盘 SSD USB 闪存盘 数据块 字符设备 (Character) 连续字符流 顺序访问 实时传输 键盘 鼠标 串口终端 A → B → C → D → E 字符流 网络设备 (Network) 数据包传输 高速实时传输 DMA 和中断机制 网络适配器 调制解调器 路由器 数据包"} />

根据设备类型的不同，我们将 [I/O 接口](https://csgraduates.com/constitution_principle/bus/io/#io-%e6%8e%a5%e5%8f%a3) 分为三种类型：

1. **块设备**：

块设备（Block Device）是计算机中一类以 **固定大小的数据块** 为单位进行数据读写的 I/O 设备，通常用于存储和检索大量数据。它们 **支持随机访问**，意味着可以直接定位到任意数据块，而无需按顺序读取。

典型例子包括[机械硬盘](https://csgraduates.com/constitution_principle/storage/storage/#%e6%9c%ba%e6%a2%b0%e7%a1%ac%e7%9b%98)（HDD）、[固态硬盘](https://csgraduates.com/constitution_principle/storage/storage/#%e5%9b%ba%e6%80%81%e7%a1%ac%e7%9b%98)（SSD）和 USB 闪存盘。**块设备** 的 *数据传输速率较高*，但涉及复杂的控制器和协议（如 SATA 或 NVMe)，因此在操作系统中通常由专门的驱动程序管理。

2. **字符设备**：

字符设备是另一类 I/O 设备，以 **连续的字符流** 形式进行数据传输，不支持随机访问，也不依赖数据块的概念。它们通常用于与硬件设备交互，如 **键盘**、**鼠标**、**串口终端** 或 **打印机**。字符设备的数据流是顺序的，适合实时或低速数据传输场景。

例如，键盘输入的字符流直接发送到系统，而无需缓存整个数据块。字符设备在操作系统中通常通过简单的驱动程序管理，且不涉及文件系统，适合处理非结构化数据。

3. **网络设备**：

网络设备是专门用于计算机网络通信的 I/O 设备，负责在计算机与网络之间传输 **数据包**。它们包括[网络适配器](https://csgraduates.com/computer_network/physical/dev/#%e7%bd%91%e7%bb%9c%e9%80%82%e9%85%8d%e5%99%a8) 和调制解调器等设备。网络设备 **以数据包为单位** 工作，通常通过协议栈（如 TCP/IP) 处理数据，支持点对点或广播通信。

与块设备和字符设备不同，网络设备更注重 **数据的高速传输** 和 **实时性**，常涉及中断机制和 **DMA**（直接内存访问）来提高效率。操作系统通过网络驱动程序管理这些设备，以实现网络连接和数据交换。

### I/O 控制方式

在计算机系统中，I/O（输入/输出）控制方式决定了 CPU 与外设之间如何协作完成数据传输。主要有三种方式，每种方式在性能、复杂度与使用场景上各有不同：

- [程序查询方式](https://csgraduates.com/constitution_principle/bus/io/#%e7%a8%8b%e5%ba%8f%e6%9f%a5%e8%af%a2%e6%96%b9%e5%bc%8f)：
  又称“轮询方式”，CPU 主动不断地检查外设状态是否准备好。在设备较少、交互不频繁的场景下实现简单，但会占用大量 CPU 时间，效率较低。
- [程序中断方式](https://csgraduates.com/constitution_principle/bus/io/#%e7%a8%8b%e5%ba%8f%e4%b8%ad%e6%96%ad%e6%96%b9%e5%bc%8f)：
  外设准备就绪后向 CPU 发出中断信号，CPU 暂停当前任务处理 I/O 操作，相比轮询更高效。适用于响应及时性要求较高的系统，能够显著降低 CPU 的无效等待。
- [DMA 方式](https://csgraduates.com/constitution_principle/bus/io/#dma-%e6%96%b9%e5%bc%8f)：
  即“直接内存访问”，在数据量大、频繁传输的场景中尤为高效。CPU 只负责启动 DMA 控制器，数据传输过程由 DMA 独立完成，显著提高系统整体效率。

三种 I/O 控制方式在 **数据流路径** 和 **CPU 参与度** 上有显著差异，可参考下图对比理解：

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-002.svg" alt={"I/O 控制方式对比（CPU 参与度 / 数据流） 方式 数据流 CPU 参与度（时间轴） 程序查询 (Polling) 同步、忙等 CPU 设备 内存 轮询/读状态 数据→CPU 寄存器 CPU 再写入内存（每字节都经 CPU） CPU 全程忙：检查 → 传送 → 检查 → … 时间 → 几乎 100% CPU 占用，效率最低 程序中断 (Interrupt) 异步、需中断响应 CPU 设备 内存 启动 I/O 完成 → 中断 + 数据 中断服务程序搬运（仍经 CPU） 时"} />

### I/O 软件层次

真题
8

选择题
[11#26](https://csgraduates.com/study_methods/408quiz/2011/#26)
[12#26](https://csgraduates.com/study_methods/408quiz/2012/#26)
[13#25](https://csgraduates.com/study_methods/408quiz/2013/#25)
[22#32](https://csgraduates.com/study_methods/408quiz/2022/#32)
[26#31](https://csgraduates.com/study_methods/408quiz/2026/#31)
[26#32](https://csgraduates.com/study_methods/408quiz/2026/#32)

解答题
[19#44](https://csgraduates.com/study_methods/408quiz/2019/#44)
[23#46](https://csgraduates.com/study_methods/408quiz/2023/#46)

I/O 软件层次是计算机系统中用于管理和协调输入输出操作的软件架构，从用户应用程序到硬件设备，分为多个层次，每一层负责特定的功能，确保高效、可靠的 I/O 操作。

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-003.svg" alt={"用户层I/O软件 设备独立性软件 设备驱动程序 中断处理程序 硬件 I/O 请求 I/O 应答 执行 I/O 操作 设置设备寄存器；检查状态 命名；保护；分块；缓冲；分配 产生 I/O 请求；格式化 I/O；SPOOLing I/O 软件层次 I/O 完成时唤醒驱动程序"} />

#### 用户层 I/O 软件

用户层 I/O 软件是 I/O 软件体系结构的最上层，直接与用户应用程序交互，**提供标准化的接口** 以便程序能够访问 I/O 设备。

它主要包括库函数和系统调用接口，例如 C 语言中的 stdio 库（提供 printf、scanf 等函数）或 POSIX 系统调用（如 read、write）。这些软件封装了底层的复杂性，让用户程序无需关心具体设备细节即可进行输入输出操作。

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-004.svg" alt={"用户层 I/O 软件 把\"简单的库调用\"翻译成\"复杂的系统调用\"，向下陷入内核 应用程序 printf(\"hello %d\", n); fgets(buf, 128, stdin); fwrite(&s, sizeof s, 1, fp); // 想的是\"输出一段话\"，不关心设备 调用库函数 用户层 I/O 软件（库 / 运行时） 运行在用户态 ① 格式化 / 解析 printf scanf sprintf atoi / strtol ② 用户级缓冲 fopen / fclo"} />

#### 设备独立性软件

设备独立性软件位于用户层和设备驱动程序之间，负责屏蔽不同 I/O 设备的硬件差异，提供统一的接口供上层软件使用。它 **实现了设备无关的功能**，例如文件系统管理、设备命名、缓冲管理和错误处理。

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-005.svg" alt={"设备独立性软件 位于用户层与设备驱动之间，屏蔽硬件差异，向上提供统一接口 用户层 I/O 软件（库函数 / 系统调用：read、write、printf…） 设备独立性软件（核心功能） ① 统一命名与接口 将不同设备抽象为文件 / 逻辑名 上层无需关心具体硬件类型 ② 缓冲管理 单缓冲 / 双缓冲 / 缓冲池 缓解 CPU 与设备速度差异 ③ 设备分配与回收 独占 / 共享 / 虚拟设备管理 避免多进程并发访问冲突 ④ 差错处理 校验、重试等通用错误处理 屏蔽底层硬件异常 ⑤"} />

例如，操作系统通过设备独立性软件将不同类型的存储设备（如硬盘、USB 驱动器）抽象为统一的文件系统接口，程序只需操作文件路径而无需关心底层设备类型。此外，它还管理设备的分配与释放，避免多个进程同时访问同一设备导致冲突。设备独立性软件的核心作用是提高系统的模块化和可移植性。

#### 设备驱动程序

设备驱动程序是 I/O 软件层次中直接 **与具体硬件设备交互** 的部分，每个设备驱动程序针对特定类型的硬件设备设计，负责将设备独立性软件的通用命令转换为设备能够理解的特定指令。

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-006.svg" alt={"设备驱动程序 承上启下：把设备独立性软件的通用命令 → 翻译为特定硬件能理解的指令 设备独立性软件（通用 I/O 命令：read / write / ioctl…） 设备驱动程序（内核态，针对具体硬件） ① 命令翻译 通用 I/O 请求 → 设备特定指令 写控制/命令寄存器 如：扇区读写、控制字符 ② 设备初始化与配置 复位、加载固件 分配 I/O 端口、DMA 通道 注册中断向量 ③ 数据传输 程序查询 / 中断 / DMA 在设备寄存器与内存间搬运 配合上层缓冲区 ④ 中"} />

例如，一个硬盘驱动程序会处理扇区读写命令，而一个打印机驱动程序可能发送控制字符序列。驱动程序通常由设备制造商提供，运行在操作系统的内核态，能够直接访问设备寄存器和端口。由于硬件设备的多样性，驱动程序需要处理设备初始化、数据传输和错误恢复等任务，是连接软件与硬件的关键桥梁。

#### 中断处理程序

[中断处理程序](https://csgraduates.com/constitution_principle/bus/io/#%e7%a8%8b%e5%ba%8f%e4%b8%ad%e6%96%ad%e6%96%b9%e5%bc%8f) 是 I/O 软件层次中 **专门处理硬件中断** 的部分，当设备完成操作或发生事件（如数据到达、错误发生）时，**硬件会触发中断信号**，中断处理程序迅速响应以处理这些事件。它运行在极低的层次，通常以极高的优先级执行，负责保存中断现场、处理设备状态、通知上层软件或触发后续操作。

<InlineSvg src="/images/operating-system/operating-system-io-device-devices-007.svg" alt={"中断处理程序大致流程 硬件触发 → 保存现场 → 快速处理 → 恢复现场 → 回到原任务 设备 / 硬件 CPU / 中断处理程序（内核态，高优先级、代码精简） 用户/上层软件 ① 设备事件发生 数据到达 / 完成 / 出错 中断信号 ② 响应中断 暂停当前任务 切换到内核态 ③ 保存现场 PC、寄存器、状态字 压栈保存 ④ 识别中断源 / 分发 查中断向量表 跳转到对应处理例程 ⑤ 执行中断服务 读写设备寄存器 搬运数据、置状态 ⑥ 唤醒/通知上层 唤醒等待进程 通知驱动/"} />

例如，当网卡接收到 **数据包** 时，中断处理程序会将数据移到内存并通知网络协议栈。由于中断处理需要极快的响应速度，代码通常高度优化且精简，尽量减少处理时间，以避免影响系统性能。

#### 硬件

硬件是 I/O 软件层次的最底层，直接负责 **执行物理 I/O 操作**，包括实际的设备控制器、寄存器、端口和数据传输通道。硬件层由具体的 I/O 设备（如硬盘、键盘、网络适配器）及其控制器组成，控制器通过寄存器和端口接收驱动程序的指令，执行数据读写或状态查询等操作。

例如，硬盘控制器通过 **DMA**（直接内存访问）将数据直接传输到内存，而网卡通过硬件队列处理 **数据包**。硬件层的性能和特性直接决定了 I/O 操作的效率，软件层的所有操作最终都依赖硬件的物理实现。
